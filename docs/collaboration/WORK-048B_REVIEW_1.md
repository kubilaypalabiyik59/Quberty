# WORK-048B review 1 — CHANGES REQUIRED

No draft has been applied. Use current original sources. Extended thinking is disabled; Codex independently reviews the returned code.

1. Schema VERIFIED: SalesOrder maps to sales_orders, with UUID id and tenant_id. Use a parameterized tagged-template SELECT id FROM sales_orders, matching id and tenant_id with ::uuid casts, FOR UPDATE. Never guess PascalCase table names. Assert table and parameterization in the mock test.
2. Transaction FIRST, lock before reload. Remove redundant pretransaction precheck. Check returned_at before status. Already-returned and invalid statuses return 409; missing/foreign404. Guards before number or stock.
3. Preserve currency refusal on BOTH paths, client:tx, before restoreIssues. Invoiced tax ctx also client:tx. Uninvoiced path has no tax/FACTURA/invoice-payment reversal. Call postIssueCogs directly for it: do not resolve generic ALL-scope COGS/INVENTORY first; the helper already resolves by item group.
4. Final salesOrder.updateMany must guard tenant/id/status/returned_at:null and count1, else409. Customer update tenant-scoped. Do not touch invoice/pay: WORK-048A failed to return a patch. Do not claim invoice/payment cross-path races are fixed. Return/return and POS void admission can share the order lock.
5. Tests contradict draft code: message 'No credit note issued' contains 'credit note'; assert correct meaning. Returned status should409. Reset mock implementations in beforeEach. Pin no stock/number on refusals and propagated journal failure. Mock transaction is not actual PostgreSQL rollback proof. Cover both invoice states, currency refusal before stock, recorded-cost provenance and guarded final write.
6. UI: remove implementation warnings about legacy accounting and invalid p inside ul. Plain messages: uninvoiced restores shipment stock/cost without credit note; invoiced restores stock, issues credit note, records return in accounting. Do not promise exact reversal of all vouchers. Hooks stay above early returns. Condition numbering query/input/payload/errors/disabled on invoice_id, preserving pending/error feedback.
7. Do not change imports for commentary. The draft unified diff invented old comments/emoji and cannot apply. Output-format clarification: return three clearly named blocks: (a) complete replacement RETURN HANDLER ONLY as typescript, starting app.post('/:id/return' and ending its closing }); (b) complete NEW salesReturnLifecycle.test.ts as typescript; (c) small frontend unified diff against exact current source. Codex mechanically converts named blocks to apply_patch using exact existing boundaries. This does not expand paths or grant tools. No shell commands or full routes-file replacement. Keep residuals brief.

Verified interfaces: ReturnSalesOrderSchema.factura_number is optional; assertDocumentCurrencySupported options and computeDocumentTax context both support client:tx. Original financial reversal limitations remain open.

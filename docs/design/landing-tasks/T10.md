# Task T10 — glass sections over the mosaic

The sections after the hero used to be opaque (`bg-bg`), which hid the new animated mosaic
field. Make the sections transparent and turn their cards into frosted glass. **This is a class
edit only.** Do not change any structure, text or logic.

Edit exactly these seven files, all of them in the chat:

- `frontend/src/app/(landing)/_components/Problem.tsx`
- `frontend/src/app/(landing)/_components/OneSystem.tsx`
- `frontend/src/app/(landing)/_components/Capabilities.tsx`
- `frontend/src/app/(landing)/_components/Flows.tsx`
- `frontend/src/app/(landing)/_components/Foundation.tsx`
- `frontend/src/app/(landing)/_components/FinalCta.tsx`
- `frontend/src/app/(landing)/_components/LandingFooter.tsx`

## The rules, applied everywhere they match

1. Every `<section>` className: remove the `bg-bg` class. Keep everything else. For example,
   `scroll-mt-16 bg-bg py-24 sm:py-32` becomes `scroll-mt-16 py-24 sm:py-32`.
2. Every element whose className contains `bg-surface`: replace `bg-surface` with
   `bg-surface/60 backdrop-blur-md`. This covers the cards, the OneSystem ledger strip, the Flows
   step chips and the FinalCta card. For example,
   `rounded-surface border border-border bg-surface p-6` becomes
   `rounded-surface border border-border bg-surface/60 backdrop-blur-md p-6`.
3. `LandingFooter`: on the `<footer>`, replace `bg-bg` with `bg-bg/70 backdrop-blur-md`.

Do not touch `Hero.tsx`, `page.tsx`, the header or any other file. Output all seven files in
full, each with its full path alone on the line before its code block.

## Done

The gate passes. In addition, `grep` for `bg-bg py` in these files must find nothing.
